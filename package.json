{
  "singleQuote": true,
  "semi": true,
  "dependencies": {
    "pino": "^10.0.0",
    "pino-http": "^11.0.0"
  },
  "devDependencies": {
    "c8": "^10.1.3",
    "eslint": "^9.37.0",
    "express-rate-limit": "^8.1.0",
    "prettier": "^3.6.2",
    "supertest": "^7.1.4"
  },
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "test": "node --test",
    "test:cov": "c8 -r text -r html node --test",
    "lint": "eslint .",
    "format": "prettier -w ."
  },
  "eslintConfig": {
    "env": {
      "es2022": true,
      "node": true
    },
    "extends": [
      "eslint:recommended"
    ]
  },
  "prettier": {
    "singleQuote": true,
    "semi": true
  }
}
